# `/lk_re-app_v1` examples

## 1. Archive-only — first visit (wiki raw template)

**User:** `/lk_re-app_v1` — reverse engineer `~/my-game` (or `work/templates/raw-donor/`)

**Agent asks once:** source path confirmed · product slug → `my-game-1` · build-through? → **no**

**Covenant (abbrev):** slug `uvrn-verification-wiki-1` · Way 2 route `archives/extractions/uvrn-verification-wiki-1/` · Avoid purple-indigo glow, Inter hero, glass stacks · Way 2 stop = kit `wiki-first-viewport` + verify exit 0 · build-through **no**

**Workflow:**
1. Trinity R1 — scout raw tree; scribe overlap matrix (Wiki tab → partial `article-read` + GAP sidebar; Monitor → GAP network mesh; Style tab → **do not archive** raw hex).
2. Mint `archives/extractions/uvrn-verification-wiki-1/` — scaffold from testplat pattern (scripts token-replace, empty libraries).
3. Extract elements + kit; update REGISTRY/MAP with provenance to raw files.
4. `npm run verify` → exit 0.
5. Mint `RE-APP.md` with `build_through: false`.
6. Ask build-through → conductor says no.

**Stop:** parts on shelf. No BPS suite. `/lk_re-app_v1` complete.

---

## 2. Build-through re-entry — days later, new chat

**User:** `/lk_re-app_v1 build-through` — extraction `uvrn-verification-wiki-1`

**Agent asks once:** slug/path confirmed · re-run verify? → yes if unsure · raw drift? → no

**Re-entry checks:**
- `archives/extractions/uvrn-verification-wiki-1/` exists
- `REGISTRY.md` + `MAP.md` present
- `npm run verify` exit 0
- Read `RE-APP.md` — `way2_complete: true`, `verify_status: pass`

**Workflow:**
1. Skip re-archive (no drift).
2. Invoke `lk_build-plan-suite_v1` → `.admin/docs/plan-suites/uvrn-verification-wiki-adopt/`
3. Write `specs/overlap-matrix.md` citing archived elements (not raw-only).
4. Write `specs/art-direction.md` with Avoid list from covenant; cite archived world, not raw chrome.
5. Optional: Mode A `admin/wiki/` stub + `/lk_gear-set_v1` if gears opted.
6. Update `RE-APP.md` → `build_through: true`.

**Stop:** paper suite on disk. Explicit: not GO, not app build.

---

## 3. Split session — Way 2 stop, Way 1 deferred

**Visit 1 (Friday):**
- `/lk_re-app_v1` + raw path · build-through no
- Way 2 complete · `RE-APP.md` minted · conductor defers adopt

**Visit 2 (Monday, new Cursor chat):**
- User pastes only: `/lk_re-app_v1 build-through uvrn-verification-wiki-1`
- Agent reads `RE-APP.md` for `source_path` + `as_of` — no raw path needed
- Verify re-run → pass → Way 1 as in example 2

**Visit 3 (separate GO, not `/lk_re-app_v1`):**
- `lk_prep-prompt_v1` for suite unit 01 — product implementation begins

---

## Anti-patterns

| Wrong | Right |
|---|---|
| `/lk_re-app_v1 build-through` before verify pass | Way 2 done gate first |
| Extend `uvrn_testing-platform_design-system_extraction-1` in place | New folder under `archives/extractions/` |
| Copy raw `#6366f1` glow into REGISTRY world tokens | Structural archive + Avoid list |
| BPS suite from raw-only overlap guesses | Cite REGISTRY/MAP from Way 2 |
| `/lk_re-app_v1` ships React app code | Archives + paper only |