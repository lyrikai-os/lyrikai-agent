# END-GOAL — lyrikai-super-agent

**Suite:** `lyrikai-super-agent`  
**Product:** `lyrikai-agent`  
**Status verb:** **paper-complete** when units 01–05 are filled — not production GO, not tip land, not git push.

---

## Stop line (human)

> Human can `git clone` / open `lyrikai-agent`, run `agent/install/install.sh`, pick **Lyrikai Super Agent** in Cursor (or `/lyrikai`), receive scene-aware route proposals with trust gates, and install verifies manifest vs disk — **without** false GO, tip writes, or requiring memorized slash commands.

Paper complete; ship of tip patches is a separate Admin GO.

---

## What “done” means (fill phase — units 01–05)

Under **`lyrikai-agent/` only:**

| # | Deliverable | Verify |
|---|-------------|--------|
| 01 | `README.md`, `LICENSE`, `wiki-pointer.md`, parent attic index | Paths exist; README states MIT scope + walls |
| 02 | `agent/AGENT-CARD.md`, `agent/LYRIKAI-BOOT.md`, `agent/MANIFEST.md` | L0 ≤80 lines; rings table complete |
| 03 | `agent/SCENE-ROUTER.md`, `agent/TRUST-GATES.md` | 15+ scene rows + P0–P3 + north-star beat gates |
| 04 | `agent/agents/lyrikai-super.md`, `agent/install/install.sh`, `agent/install/verify.sh` | `verify.sh` exit 0 after install |
| 05 | `lyrikai-meta` proposal + catalog row + README pointer | Wiki paths cite `lyrikai-agent/` |

Plus this suite package (BPS Step A) — README, END-GOAL, 5 unit plans, 3 specs, 2 guides.

---

## Explicitly not done (out of phase)

- Git commit / push to [github.com/lyrikai-os/lyrikai-agent](https://github.com/lyrikai-os/lyrikai-agent)
- Tip T2 writes (`factory/skills/lyrikai/SKILL.md` patch, `ADMIN-SKILLS.md` ring column, `ORCHESTRATION.md` §8)
- Vendoring full skill bodies into `lyrikai-agent`
- WORKSTREAMS / `.hive/` on this leaf (no board row)
- `/gear-set` — docs-only product; gears skipped unless conductor opts in
- Claiming Lyrikai is Admin, Audit, or Orchestrator

---

## Success test

Fresh agent: reads suite README → executes unit 01 → walks units in order → `verify.sh` passes → scene router proposes delegate with P0–P3 honesty → trust gates block false GO → MANIFEST rings match tip `ADMIN-SKILLS.md` phone book.

---

## After this lands

**Stop at paper.** Later ask-first: conductor GO for git push; Admin GO for tip skill patch (T2); optional unit 06 dogfood; optional `.hive/` + Paper row if program decision adds Hive SoR.
